import { BlobsanitizerPipe } from './blobsanitizer.pipe';

describe('BlobsanitizerPipe', () => {
  it('create an instance', () => {
    const pipe = new BlobsanitizerPipe();
    expect(pipe).toBeTruthy();
  });
});
